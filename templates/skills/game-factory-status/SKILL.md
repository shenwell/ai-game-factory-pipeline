---
name: game-factory-status
description: Human summary and JSON status from game-factory CLI.
---

# game-factory-status

Run `game-factory status` and summarize phase, blockers, open tasks count, last gate results under `.game-factory/jobs/`. If phase is `bootstrap`, also run `game-factory onboard`.
