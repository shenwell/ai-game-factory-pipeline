# Cursor adapter

Installed artifacts:

- `.cursor/commands/game-factory-*.md` — invoke matching skill in `.agents/skills/`
- `.cursor/rules/game-factory.mdc`

No workflow duplication in command files.
